<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asmita.lpractice">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" /-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.asmita.lpractice.LPracticeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label=""
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <!-- android:label="Lets Practise"
        android:label="@string/app_name" -->
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- android:label="@string/app_name" -->
        <activity
            android:name=".ReferenceLoginActivity"
            android:screenOrientation="portrait" />
        <!-- android:label="@string/title_activity_login" /> -->
        <activity
            android:name=".SelectChapter"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- android:label="@string/title_activity_select_chapter" -->
        <activity
            android:name=".MCQActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- android:label="@string/title_activity_mcq" -->
        <activity
            android:name=".Result"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--android:label="@string/title_activity_result"-->
        <activity
            android:name=".DashboardActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ShowRating"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--android:label="@string/title_activity_show_rating"-->
    </application>

</manifest>